# Database
spring.datasource.url=jdbc:mysql://mysql:3306/mydb?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=password
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=update  
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.show-sql=true

# File upload
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# MinIO
minio.url=http://minio:9000
minio.access-key=minioadmin
minio.secret-key=minioadmin
minio.bucket=profiles
minio.public-host=http://localhost:9000

# JWT
jwt.secret=u/PjdcwVzCGmlHX7b5VnF4MFhG601Am/TIBMgtFWql8=

# Server
server.port=8080

# Person detector
person.detector.url=http://person-detector:8000/detect_person

# Logging
logging.level.com.example=INFO
logging.level.org.springframework.security=INFO

# Kafka
spring.kafka.bootstrap-servers=kafka:9092
cluster.url=http://person-detector:8000/cluster
# Redis
spring.redis.host=redis
spring.redis.port=6379
spring.devtools.restart.enabled=true
spring.kafka.consumer.group-id=ride-group
spring.kafka.consumer.auto-offset-reset=earliest